version: 2
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run:
          name: Install test requirements
          command: |
            virtualenv ~/.venv
            . ~/.venv/bin/activate
            pip install -r requirements.txt
      - run:
          name: Run molecule tests
          command: |
            . ~/.venv/bin/activate
            molecule --debug test
